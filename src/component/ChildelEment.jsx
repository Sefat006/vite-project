// import React from 'react';

const ChildElement = (props) => {
    return (
        <div>
            <h1> {props.title} </h1>
            <h1> {props.des} </h1>
        </div>
    );
};

export default ChildElement;